name: Build. Deploy.

on:
  push:
    branches:
      - main

env:
  GOLDEN_KEY: ${{ secrets.GOLDEN_KEY }}
  USER_AGENT: ${{ secrets.USER_AGENT }}
  ENABLED: ${{ secrets.ENABLED }}
  TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
  SECRET_KEY: ${{ secrets.SECRET_KEY }}

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Build Docker image
        run: docker build -t funpay_bot_image .

      - name: Run Docker container
        run: docker run --name funpay_bot funpay_bot_image

      - name: Wait for bot to initialize
        run: sleep 10

      - name: Checking logs
        run: docker logs funpay_bot

#  deploy:
#    needs: build-test
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v2
#
#      - name: Copy code via ssh
#        uses: appleboy/scp-action@master
#        with:
#          host: ${{ secrets.HOST }}
#          username: ${{ secrets.USER }}
#          key: ${{ secrets.SSH_KEY }}
#          passphrase: ${{ secrets.SSH_PASSPHRASE }}
#          source: "."
#          target: "/home/ubuntu/telegram_bots/funpay_bot"
#
#      - name: Executing remote ssh commands to deploy
#        uses: appleboy/ssh-action@master
#        with:
#          host: ${{ secrets.HOST }}
#          username: ${{ secrets.USER }}
#          key: ${{ secrets.SSH_KEY }}
#          passphrase: ${{ secrets.SSH_PASSPHRASE }}
#          script: |
#            cd /home/ubuntu/telegram_bots/funpay_bot
#            sudo docker stop funpay_bot
#            sudo docker build -t funpay_bot_image .
#            sudo docker run -d --restart=unless-stopped --name funpay_bot funpay_bot_image
#            sudo docker image prune -f
